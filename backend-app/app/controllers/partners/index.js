const {createPartner} = require('./createPartner')
const {deletePartner} = require('./deletePartner')
const {getAllPartners} = require('./getAllPartners')
const {getPartner} = require('./getPartner')
const {updatePartner} = require('./updatePartner')
const {updateClick} = require('./updateClick')

module.exports = {
  createPartner,
  deletePartner,
  getAllPartners,
  getPartner,
  updatePartner,
  updateClick
}
