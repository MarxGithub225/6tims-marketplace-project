const {createIcon} = require('./createIcon')
const {deleteIcon} = require('./deleteIcon')
const {getAllIcons} = require('./getAllIcons')

module.exports = {
  createIcon,
  deleteIcon,
  getAllIcons
}
